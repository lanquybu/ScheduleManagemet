<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền dialog -->
    <solid android:color="@android:color/white" />

    <!-- Bo góc -->
    <corners android:radius="16dp" />

    <!-- Nếu muốn viền -->
    <stroke
        android:width="1dp"
        android:color="@color/softBlue" />

</shape>
